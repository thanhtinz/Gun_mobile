# CI Android debug keystore (NOT for Play Store)

Alias: `gunmobile`  
Store/key password: `android`

GitHub Actions uses this file unless you override with secrets
`ANDROID_KEYSTORE_PASS` / `ANDROID_KEYALIAS_NAME` / `ANDROID_KEYALIAS_PASS`
and replace the keystore.
